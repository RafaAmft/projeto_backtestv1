# Sistema Robusto de Obtenção de Dados Financeiros
# Versão: 1.0
# Data: 07/07/2025

# Dependências principais
requests>=2.31.0
yfinance>=0.2.28
pandas>=2.0.0
numpy>=1.24.0
PyYAML>=6.0.1

# APIs financeiras
alpha-vantage>=2.3.1
twelvedata>=1.0.0
python-binance>=1.0.19

# Cache e armazenamento
redis>=5.0.0
sqlite3  # Incluído no Python padrão

# Processamento de dados
scipy>=1.11.0
scikit-learn>=1.3.0

# Logging e monitoramento
structlog>=23.2.0
prometheus-client>=0.17.0

# Testes
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Desenvolvimento
black>=23.7.0
flake8>=6.0.0
mypy>=1.5.0

# Utilitários
python-dateutil>=2.8.2
pytz>=2023.3
tqdm>=4.65.0

# Web scraping (para fontes alternativas)
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.15.0

# Compressão e serialização
gzip  # Incluído no Python padrão
pickle  # Incluído no Python padrão
hashlib  # Incluído no Python padrão

# Threading e concorrência
threading  # Incluído no Python padrão
concurrent.futures  # Incluído no Python padrão

# Sistema de arquivos
pathlib  # Incluído no Python padrão
shutil  # Incluído no Python padrão

# Tipagem
typing-extensions>=4.7.0

# Configuração
python-dotenv>=1.0.0

# Validação de dados
pydantic>=2.0.0
marshmallow>=3.20.0

# Cache avançado
cachetools>=5.3.0
diskcache>=5.6.0

# Monitoramento de performance
psutil>=5.9.0
memory-profiler>=0.61.0

# Backup e compressão
tarfile  # Incluído no Python padrão
zipfile  # Incluído no Python padrão

# Segurança
cryptography>=41.0.0
hashlib  # Incluído no Python padrão

# Interface web (opcional)
flask>=2.3.0
dash>=2.14.0
plotly>=5.17.0

# Documentação
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0 